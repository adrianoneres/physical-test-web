import { styled } from '..';

export const Container = styled('main', {
  maxWidth: 450,
  margin: '0 auto',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'flex-start',
  justifyContent: 'center',
  minHeight: '100vh',
});
